{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "acceptedTypes": {
      "type": "array",
      "description": "Content types the API accepts (may use MIME types or extension names)",
      "items": { "type": "string" },
      "default": ["application/json"]
    },
    "fileUploadMaxFileSize": {
      "type": "number",
      "description": "Limit the size of any uploaded file",
      "default": 524288000
    },
    "fileUploadTempDir": {
      "type": "string",
      "description": "The directory that uploaded files are stored in"
    }
  }
}